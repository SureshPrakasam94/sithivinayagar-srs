/* ============================================================
   ⚙️ YOUR SETTINGS — edit this file only
   ============================================================ */
window.VG_CONFIG = {
  /* Website admin login (gate for the admin page only). */
  ADMIN_USER: 'admin',
  ADMIN_PASS: 'VNG@2026',

  /* ---- GitHub mode (optional) ----
     Set 'username/repo' of THIS project's repository to enable
     writing data/donors.xlsx straight into the GitHub project
     (first-time upload from your PC, then a commit per change).
     Leave '' to use the local data-folder mode instead.
     NOTE: the GitHub TOKEN is NOT stored here — you type it on the
     admin page at runtime (kept only in your browser session).   */
  GITHUB_REPO: 'SureshPrakasam94/sithivinayagar-srs',
  GITHUB_BRANCH: 'main'
};
